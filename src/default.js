import React from 'react';
import {
    Typography,
} from "@mui/material";
import { styled } from '@mui/system';

function default () {
    return (
        <Typography></Typography>
    )
}

export default default ;


//const  = styled()({{})